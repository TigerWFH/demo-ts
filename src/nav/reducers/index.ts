function handleActions(state = {}, action: any = {}) {
    switch (action.type) {
        case 'SIGN_ON':
            return;
        case 'SIGN_UP':
            return;
        default:
    }
}

export default handleActions;
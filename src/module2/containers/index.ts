import { connect } from 'react-redux';
import { Module2 } from '../index';

function mapStateToProps() {
    return {};
}
function mapDispatchToProps() {
    return {};
}
export default connect(mapStateToProps, mapDispatchToProps, null, null)(Module2);
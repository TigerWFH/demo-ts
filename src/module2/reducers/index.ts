let handleActions = (state = {}, action: any) => {
    switch (action.type) {
        case 'SIGN_ON':
            break;
        case 'SIGN_UP':
            break;
    }

    return state;
}

export default handleActions;